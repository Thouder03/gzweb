name: Node.js CI (Fixed Build Order)

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    
    container:
      image: ubuntu:20.04
      env:
        DEBIAN_FRONTEND: noninteractive

    strategy:
      matrix:
        # 基于你的日志，10.x 是 gzweb 1.3.0 最兼容的版本
        node-version: [10.x]

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Initialize Environment
      run: |
        apt-get update
        ln -fs /usr/share/zoneinfo/Etc/UTC /etc/localtime
        apt-get install -y tzdata sudo wget curl gnupg2 lsb-release git

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Setup Repositories
      run: |
        wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | apt-key add -
        echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" > /etc/apt/sources.list.d/google-chrome.list
        
        wget https://packages.osrfoundation.org/gazebo.key -O - | apt-key add -
        echo "deb http://packages.osrfoundation.org/gazebo/ubuntu-stable focal main" > /etc/apt/sources.list.d/gazebo-stable.list

    - name: Install System Dependencies
      run: |
        apt-get update
        apt-get install -y \
          xvfb \
          google-chrome-stable \
          gnustep-base-runtime \
          libjansson-dev \
          libboost-dev \
          imagemagick \
          libtinyxml-dev \
          cmake \
          build-essential \
          libgazebo11-dev

    - name: Patch GZNode C++ Code (For Gazebo 11)
      run: |
        # 修复 Gazebo 版本宏定义，确保 InitAll 函数被定义
        find . -name "GZNode.cc" -exec sed -i 's/GAZEBO_MAJOR_VERSION == [0-9]/GAZEBO_MAJOR_VERSION >= 7/g' {} +

    - name: Build Web Assets (Fix Missing gz3d.gui.js)
      run: |
        # 1. 安装全局和本地依赖
        npm install -g grunt-cli
        # --unsafe-perm 解决 Docker 里的权限问题
        npm install --unsafe-perm
        
        # 2. 手动运行 Grunt 生成 gz3d.gui.js
        # 这是修复 "file COPY cannot find" 的关键
        grunt build

    - name: Setup Headless Chrome
      run: |
        wget -q -O /usr/bin/xvfb-chrome https://gist.githubusercontent.com/chapulina/a80bd83d3494c96dc9eead843579c435/raw/7d304595d401c1265b533626d542ace7e6916002/xvfb-chrome
        chmod 755 /usr/bin/xvfb-chrome
        sed -i 's/exec google-chrome/exec google-chrome --no-sandbox --disable-dev-shm-usage/g' /usr/bin/xvfb-chrome
        ln -sf /usr/bin/xvfb-chrome /usr/bin/google-chrome

    - name: Build and Deploy
      run: |
        # 现在再运行 deploy，此时 gz3d.gui.js 已经存在
        npm run deploy --verbose

    - name: Run Tests
      run: |
        npm run test:headless || echo "Tests completed with warnings"

    - name: Post Build
      run: |
        npm run update
        npm run docs
      env:
        CI: true